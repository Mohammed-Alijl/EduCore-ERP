{{--
    Finance Empty State Component

    Displays a friendly empty state message when no data is available.
--}}
<div class="finance-empty-state">
    <div class="finance-empty-icon">
        <i class="las {{ $icon }}"></i>
    </div>
    <p class="finance-empty-message">{{ $message }}</p>
</div>
